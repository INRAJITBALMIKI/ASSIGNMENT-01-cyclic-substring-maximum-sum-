# ASSIGNMENT-01-cyclic-substring-maximum-sum-
This problem involves finding the maximum sum of character values from a substring of a given string. Each character is assigned a value based on its position in the alphabet (a = 1, b = 2, …, z = 26). The substring must contain only unique characters, meaning no character can appear more than once.  
